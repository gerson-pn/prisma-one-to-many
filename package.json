{
  "devDependencies": {
    "prisma": "^6.6.0"
  },
  "scripts": {
    "dev": "cls && prisma generate && prisma migrate reset --force && prisma migrate dev && tsc && node ./src/js/index.js"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0"
  },
  "prisma": {
    "schema": "./prisma/schema/"
  }
}
